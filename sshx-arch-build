#!/usr/bin/env bash
# =========================================================
# esey-ssh-dev — Portable Arch Aware Clean Build Script
# Author: Sumit
# =========================================================

set -Eeuo pipefail

trap 'rc=$?; echo "❌ Error on line $LINENO. Exit code: $rc"; exit $rc' ERR

IFS=$'\n\t'

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
GUI="$PROJECT_ROOT/gui"
LIB="$GUI/lib"
BIN="$PROJECT_ROOT/bin"

echo "📁 Project root: $PROJECT_ROOT"

echo "🔎 Detecting architecture..."
ARCH=$(uname -m)
echo "Architecture: $ARCH"

# ---------------- Dependency Check ----------------

need() {
  command -v "$1" >/dev/null 2>&1 || {
    echo "❌ $1 not installed."
    exit 1
  }
}

echo "📦 Checking build dependencies..."

need go
need python3
need pip3

# venv check
python3 -m venv --help >/dev/null 2>&1 || {
  echo "❌ python3-venv not installed."
  exit 1
}

# tkinter check
if ! python3 - <<EOF >/dev/null 2>&1
import tkinter
EOF
then
  echo "❌ python3-tk not installed."
  exit 1
fi

echo "✅ All required dependencies found."

# ---------------- Clean Old Binaries ----------------

echo "🧹 Removing old binaries..."

mkdir -p "$BIN"
rm -f "$BIN/sshx" \
      "$BIN/sshx-dev" \
      "$BIN/sshx-key" \
      "$GUI/sshx-gui" \
      "$LIB/git-auth" \
      "$LIB/sshx-cpy" \
      "$LIB/sshx-reset"

# ---------------- Build Go Binaries ----------------

echo "🚀 Building Go binaries..."

cd "$PROJECT_ROOT"
./build-sshx
./build-init
./build-keyset

cd "$LIB"
./build-git-auth
./build-sshx-cpy
./build-reset

# ---------------- Python Build ----------------

echo "🐍 Creating virtual environment..."

cd "$PROJECT_ROOT"
rm -rf .venv
python3 -m venv .venv
source .venv/bin/activate

pip install --upgrade pip pyinstaller

echo "🖥 Building GUI..."
cd "$GUI"
./build-gui

echo "📦 Moving gui binaries..."
mv -f "$GUI/dist/sshx-gui" "$GUI/" 2>/dev/null || true
rm -rf "$GUI/dist" "$GUI/build" "$GUI/sshx-gui.spec"

# ---------------- Cleanup ----------------

echo "🧼 Cleaning virtual environment..."

deactivate 2>/dev/null || true
rm -rf "$PROJECT_ROOT/.venv"

if command -v tree >/dev/null 2>&1; then
  tree "$PROJECT_ROOT"
else
  echo "📁 Build complete."
fi

echo ""
echo "🔍 Verifying build artifacts..."

artifacts=(
  "$BIN/sshx"
  "$BIN/sshx-dev"
  "$BIN/sshx-key"
  "$GUI/sshx-gui"
  "$LIB/git-auth"
  "$LIB/sshx-reset"
  "$LIB/sshx-cpy"
)

for file in "${artifacts[@]}"; do
  if [[ ! -f "$file" ]]; then
    echo "❌ Missing artifact: $file"
    exit 1
  fi

  if [[ ! -s "$file" ]]; then
    echo "❌ Artifact exists but empty: $file"
    exit 1
  fi
done

echo ""
echo "✅ All build artifacts verified."
echo ""
echo "✅ Clean portable build successful!"
echo ""
